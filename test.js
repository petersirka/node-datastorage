var DataStorage = require('./index');
var db = new DataStorage('/users/petersirka/desktop/test/datastorage/');

setTimeout(function() {

	//console.log(db.insert({ firstname: 'Lucia', lastname: 'Širková', age: 32 }));
	//db.refresh(null);

}, 500);
